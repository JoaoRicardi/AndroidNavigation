<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/navigation"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.learnnav.HomeFragment"
        android:label="HomeFragment" >

    </fragment>

    <custom_dialog
        android:id="@+id/custom_dialog" />

    <action
        android:id="@+id/actionShowDialog"
        app:destination="@id/custom_dialog" />

    <custom_bottom_sheet
        android:id="@+id/customBottomSheet"
        />

    <action
        android:id="@+id/actionShowBottomSheet"
        app:destination="@+id/customBottomSheet"
        />

</navigation>